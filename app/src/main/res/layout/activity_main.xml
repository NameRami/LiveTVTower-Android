<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0a0e27"
    android:padding="0dp">

    <!-- Header with gradient backdrop -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/header_gradient"
        android:padding="16dp"
        android:paddingBottom="20dp">

        <TextView
            android:id="@+id/appTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="🎬 LiveTV Tower"
            android:textColor="#FFFFFF"
            android:textSize="32sp"
            android:textStyle="bold"
            android:paddingBottom="8dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Stream thousands of live channels"
            android:textColor="#B0B5FF"
            android:textSize="13sp"
            android:alpha="0.8" />

    </LinearLayout>

    <!-- Search Bar with Icon -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:paddingVertical="12dp"
        android:background="#0a0e27">

        <EditText
            android:id="@+id/searchInput"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="48dp"
            android:hint="🔍 Search channels, countries..."
            android:textColor="#FFFFFF"
            android:textColorHint="#7A7E9F"
            android:background="@drawable/search_bar_bg"
            android:paddingHorizontal="16dp"
            android:singleLine="true"
            android:textSize="14sp" />

    </LinearLayout>

    <!-- Country Filter Chips (Horizontal Scroll) -->
    <HorizontalScrollView
        android:id="@+id/countryFilterContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:background="#0a0e27">

        <LinearLayout
            android:id="@+id/countriesChipsLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingEnd="8dp" />

    </HorizontalScrollView>

    <!-- Channel Count Badge -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:background="#0a0e27">

        <TextView
            android:id="@+id/channelCountBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Showing 0 channels"
            android:textColor="#B0B5FF"
            android:textSize="12sp"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            android:background="@drawable/badge_bg" />

    </LinearLayout>

    <!-- Channels RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/channelRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:clipToPadding="false" />

</LinearLayout>
